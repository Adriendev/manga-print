import React from "react";
import Calendar from "../components/Calendar";

const CalendarPage = () => {
  return (
    <section id="calendar">
      <Calendar />
    </section>
  );
};

export default CalendarPage;
